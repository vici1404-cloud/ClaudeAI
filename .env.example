# Copy to .env and fill in. Only EXPO_PUBLIC_* variables exist in the
# client bundle — never put secrets (service keys, AI keys) here.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_SENTRY_DSN=
